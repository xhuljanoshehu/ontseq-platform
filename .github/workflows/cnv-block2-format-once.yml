name: CNV block 2 format once

on:
  push:
    branches:
      - feat/cnv-validation-program
    paths:
      - ".github/workflows/cnv-block2-format-once.yml"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/cnv-validation-program
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install Ruff
        run: python -m pip install "ruff==0.16.8"
      - name: Apply canonical formatting
        run: |
          ruff format \
            src/ontseq_platform/cnv_validation_evidence.py \
            tests/test_cnv_validation_evidence.py
          ruff check \
            src/ontseq_platform/cnv_validation_evidence.py \
            tests/test_cnv_validation_evidence.py
      - name: Commit formatting if needed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            src/ontseq_platform/cnv_validation_evidence.py \
            tests/test_cnv_validation_evidence.py
          if git diff --cached --quiet; then
            echo "Block 2 evidence files already Ruff-formatted."
          else
            git commit -m "style(cnv): canonicalize block 2 evidence formatting"
            git push origin HEAD:feat/cnv-validation-program
          fi
