name: CNV block 3 schema once

on:
  push:
    branches:
      - feat/cnv-validation-program
    paths:
      - ".github/workflows/cnv-block3-schema-once.yml"

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/cnv-validation-program
      - uses: actions/setup-python@v7
        with:
          python-version: "3.11"
      - name: Install ONTSeq
        run: python -m pip install -e .
      - name: Generate versioned schemas
        run: python scripts/export_schemas.py
      - name: Commit Block 3 schemas
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            schemas/cnv-validation-cohort.schema.json \
            schemas/cnv-validation-registration.schema.json \
            schemas/cnv-validation-report.schema.json
          if git diff --cached --quiet; then
            echo "Block 3 schemas already current."
          else
            git commit -m "schema(cnv): freeze block 3 metric contracts"
            git push origin HEAD:feat/cnv-validation-program
          fi
