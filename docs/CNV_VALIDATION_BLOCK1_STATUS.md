# CNV validation Block 1 status

Research Use Only.

Current phase: TDD RED.

The feature branch currently contains the approved design, the implementation plan, and the first failing contract test for the CNV validation matrix. Production contract code has intentionally not been added yet.

This branch must not be merged while the RED phase is active.
